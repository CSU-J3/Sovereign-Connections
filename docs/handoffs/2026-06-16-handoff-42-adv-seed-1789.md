# Handoff #42 — ADV/IAPD collector: seed 1789 Capital (CRD 335007), worked pass

**Repo:** Sovereign-Connections
**Path target:** `docs/handoffs/2026-06-16-handoff-42-adv-seed-1789.md`
**Depends on:** Handoff #35 (ADV ingest + candidate emission) and #36 (package plumbing: wrapper, discover stub, regression guard, CI). Source spec for surfaces, tables, and fields is `docs/collectors/adv-iapd-discovery.md` §8. Branch off `main`.
**Type:** build / data. Adds CRD 335007 to the collector's covered-adviser seed list, runs the ingest, extends `web/data/candidates.json`, and commits the covered-adviser inventory doc. No `web/lib/types.ts` change, no `records.json` change. Candidates are the pipeline layer; promotion to records is separate.

## What this builds

The second seeded adviser for the ADV collector. Affinity (CRD 315482) was the first, in #35. The seed is justified by the covered-adviser inventory committed here: Donald Trump Jr. is a partner in 1789 Capital and is named in its Form ADV (shorthand DTJ), a qualifying financial interest under the named-family prong of the covered-persons definition.

The proxy limit from #35 stands. ADV gives ownership shape, never sovereign identity, so any emitted candidate carries the proxy signal and stays unreviewed until a human promotes it with secondary-source identity work.

## Scope guardrails (apply, carried over from #35)

- Sequential `CAND-###` continuing from the current max on disk (last reported CAND-171; confirm and start at the next integer, no gaps). No semantics in the ID; provenance lives in `source_filing`.
- Conservative over-emission. One candidate per private fund reporting non-US ownership. No filtering on the size of the non-US share at collection; that judgment is promotion-time.
- `business_id: null`. `promotion_status: "unreviewed"`.

## What Code needs to do

### Step 1: Commit the inventory doc

Place the covered-adviser inventory at `docs/references/covered-adviser-inventory.md` (Corey provides it). It records the seed list and the evaluated-not-seeded set, with Feinberg/Cerberus as the worked exclusion.

### Step 2: Add the seed

Add CRD 335007 (1789 Capital Management, LLC) to the collector's covered-adviser seed list. Confirm the seed-list location on disk from the #36 plumbing, and follow exactly how CRD 315482 (Affinity) is registered. Do not hardcode a path from this handoff; read the actual structure first.

### Step 3: Ingest the ADV

Run the ADV ingest on CRD 335007 against its current-period filing. Confirm the current filing date from IAPD (`adviserinfo.sec.gov/firm/summary/335007`). Assemble the per-firm object per discovery doc §8: Item 5 totals (regulatory AUM `Q5F2C`, non-US AUM `Q5F3`) and the Schedule D 7.B funds (fund name, gross asset value, percent non-US owners, beneficial-owner count).

Likely difference from Affinity, handle and flag rather than fail: 1789 brands as a venture-capital adviser and may be a venture-capital exempt reporting adviser (ERA), not a registered adviser. An ERA files a partial Form ADV, so Item 5 fields (especially `Q5F3` non-US AUM) may be sparse or absent while Schedule D 7.B is still present. If Item 5 fields are missing, do not fail; record what is present, use Schedule D 7.B for the per-fund non-US ownership, and flag the ERA case so the ingest's handling of partial filings is on record.

### Step 4: Emit candidates (worked pass)

Over CRD 335007, emit one candidate per 1789 private fund reporting non-US ownership, continuing `CAND-###` from the current max. Each candidate:

- `disclosure_type`: `adv_private_fund`.
- `source_filing`: `source` "adv_iapd", CRD 335007, SEC file number (confirm from the ADV), filing date, table/source pointer. Mirror the Affinity candidates' `source_filing` shape.
- `raw_value`: a structured object copying the fund's 7.B fields verbatim (gross asset value, percent non-US owners, beneficial-owner count, fund identifiers). Copy from the parse, never retype.
- `filer`: "1789 Capital Management, LLC".
- `business_name`: the private fund name as it appears in `IA_Schedule_D_7B1`.
- `business_id`: null. `promotion_status`: "unreviewed".
- `scope_hypothesis`: the only hand-authored field. Hedged, carrying the proxy reasoning (percent non-US, owner concentration, fund scale) and stating plainly that owner identities are not in the ADV and the sovereign source is unverified. Never assert a sovereign connection.

If 1789 reports no fund with non-US ownership, emit nothing and report the null explicitly. A documented null is a valid outcome for a seeded adviser, not a failure: it means 1789's reported ownership is domestic and the adviser surfaces no candidate.

### Step 5: Verify before reporting back

- `web/data/candidates.json` is valid JSON.
- New IDs continue sequentially from the current max with no gaps.
- Every new `business_id` is null; every new `promotion_status` is "unreviewed".
- Spot-check any 2 new candidates back to their source ADV rows; `raw_value` matches the ingest, not a paraphrase.
- No `scope_hypothesis` states a sovereign connection as fact.
- Existing candidates (the OGE set plus the three Affinity candidates) are untouched.

### Step 6: Commit

Single commit: the seed-list change, `web/data/candidates.json`, the inventory doc, and this handoff. Match the existing commit-message convention. Suggested message:

```
data(collectors): seed ADV collector with 1789 Capital (CRD 335007), worked pass + covered-adviser inventory (#42)
```

Do not push unless asked.

## Flag back, do not decide

- If the ingest cannot reproduce 1789's reported figures from the live ADV, stop and flag. Do not adjust numbers to fit.
- The ERA-versus-registered-adviser question and any partial-filing handling it forces.
- If the #36 seed-list mechanism is not where expected, surface it rather than inventing a location.
- Any candidate-schema field that 1789's rows do not map to cleanly (umbrella or affiliate fund naming, etc.).

## Out of scope

- Promotion of any emitted candidate to an SC-### record. That needs the sovereign-source identity work and stays unreviewed here.
- The Bessent and Lutnick divestiture verification (covered-adviser inventory follow-up).
- Expanding the seed list beyond 1789.
- `records.json` and `web/lib/types.ts` changes.

## After this lands

- If 1789 emits candidates, they wait for the sovereign-source identity work before any promotion (the same proxy wall, and 1789's "parallel economy" branding makes a domestic-only LP base plausible).
- If the run is a null, the ADV collector is tapped for current covered persons until the seed list grows: a new appointee or family member entering the adviser business, or the Bessent/Lutnick verification reopening the appointee prong.

---

read docs/handoffs/2026-06-16-handoff-42-adv-seed-1789.md and follow
